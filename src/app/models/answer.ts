export class ANSWER {
    public from: string;
    public to: string;
    public client: string;
    constructor(from: string, to: string, client: string) {
      this.from = from,
      this.to = to,
      this.client = client
  }
  }