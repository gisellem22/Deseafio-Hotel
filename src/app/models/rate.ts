export class RATE {
   
    public weekDay: number;
    public weekEnd: number;
    
      constructor(weekDay: number, weekEnd: number) {
          this.weekDay = weekDay,
          this.weekEnd = weekEnd
      }
  }